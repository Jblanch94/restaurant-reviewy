import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles((theme) => ({
  restaurantHeader: {
    textAlign: 'center',
  },
  gridContainer: {
    flex: 1,
  },
}));
